playwright==1.50.0
dnspython==2.6.1
beautifulsoup4==4.12.3
lxml==6.1.0
# selectolax (lexbor C engine) backs services/scraper/html.py — the fast,
# low-RSS parser for the CSS-selector adapters. bs4+lxml kept for XML/RSS
# feeds (weworkremotely) which selectolax cannot parse.
selectolax==0.3.21
httpx==0.27.0
openai==1.30.1
structlog==24.1.0
# AI dependencies (shared with ai service)
ollama==0.2.1
pinecone-client==3.2.2
