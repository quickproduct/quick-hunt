apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: job-hunter
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: k3d-jh-registry:5050/jh-api:local
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            # The API owns the mounted log files used by admin log endpoints.
            # Workers log to stdout through the ConfigMap instead.
            - name: LOG_TO_FILE
              value: "true"
          envFrom:
            - configMapRef:
                name: job-hunter-config
            - secretRef:
                name: job-hunter-secrets
          volumeMounts:
            - name: app-logs
              mountPath: /app/backend/logs
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
      volumes:
        - name: app-logs
          hostPath:
            path: /root/job-automation/backend/logs
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: job-hunter
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
