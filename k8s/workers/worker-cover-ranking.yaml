apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-cover-ranking
  namespace: job-hunter
  labels:
    app: worker-cover-ranking
    worker-type: cover-letter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-cover-ranking
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: worker-cover-ranking
    spec:
      terminationGracePeriodSeconds: 1980
      initContainers:
        - name: wait-for-redis
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z redis 6379; do echo "waiting for redis..."; sleep 2; done']
          resources:
            requests:
              memory: "16Mi"
              cpu: "10m"
            limits:
              memory: "32Mi"
              cpu: "50m"
      containers:
        - name: worker
          image: k3d-jh-registry:5050/jh-worker-lightweight:local
          imagePullPolicy: Always
          command:
            - celery
            - -A
            - services.scraper.celery_app
            - worker
            - --loglevel=warning
            - --concurrency=1
            - -Q
            - jh_cover_letter_ranking
            - -Ofair
            - -E
            - --without-mingle
            - --without-gossip
            - --max-tasks-per-child=100
            - --hostname=cover-ranking-%(n)s@%h
          env:
            - name: SERVICE_NAME
              value: "worker-cover-ranking"
          envFrom:
            - configMapRef:
                name: job-hunter-config
            - secretRef:
                name: job-hunter-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "200m"
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: worker-cover-ranking-scaler
  namespace: job-hunter
spec:
  scaleTargetRef:
    name: worker-cover-ranking
  pollingInterval: 10
  cooldownPeriod: 300
  # Ranking is triggered manually/rarely — scale to zero when idle.
  # KEDA activates 0→1 on the first queued message (any depth ≥ 1).
  minReplicaCount: 0
  maxReplicaCount: 1
  triggers:
    - type: rabbitmq
      metadata:
        protocol: http
        queueName: jh_cover_letter_ranking
        mode: QueueLength
        value: "5"
      authenticationRef:
        name: keda-trigger-auth-rabbitmq
