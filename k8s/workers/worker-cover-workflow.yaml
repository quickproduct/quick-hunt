apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-cover-workflow
  namespace: job-hunter
  labels:
    app: worker-cover-workflow
    worker-type: cover-letter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-cover-workflow
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: worker-cover-workflow
    spec:
      terminationGracePeriodSeconds: 1980
      containers:
        - name: worker
          image: k3d-jh-registry:5050/jh-worker-lightweight:local
          command:
            - celery
            - -A
            - services.scraper.celery_app
            - worker
            - --loglevel=warning
            - --concurrency=4
            - -Q
            - jh_cover_letter_workflow
            - -Ofair
            - -E
            - --without-mingle
            - --without-gossip
            - --max-tasks-per-child=100
            - --hostname=cover-workflow-%(n)s@%h
          env:
            - name: SERVICE_NAME
              value: "worker-cover-workflow"
          envFrom:
            - configMapRef:
                name: job-hunter-config
            - secretRef:
                name: job-hunter-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "200m"
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: worker-cover-workflow-scaler
  namespace: job-hunter
spec:
  scaleTargetRef:
    name: worker-cover-workflow
  pollingInterval: 10
  cooldownPeriod: 20
  minReplicaCount: 1
  maxReplicaCount: 6
  triggers:
    - type: rabbitmq
      metadata:
        protocol: http
        queueName: jh_cover_letter_workflow
        mode: QueueLength
        value: "5"
      authenticationRef:
        name: keda-trigger-auth-rabbitmq
